# PM Intelligence V2 — Python Dependencies
# Used by: document_parser and graphrag_indexer microservices
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# Document Parsing
unstructured>=0.13.0
pdfminer.six>=20221105
pi-heif
unstructured-inference
pdf2image
openpyxl>=3.1.0
python-pptx>=0.6.23
python-docx>=1.1.0

# HTTP client (for calling TypeScript services)
httpx>=0.27.0

# Embeddings (optional — for local embedding models)
# sentence-transformers>=3.0.0

# Microsoft GraphRAG (Python 3.11 compatible; skip on 3.12+)
graphrag>=0.3.0; python_version < "3.12"

# Utilities
pydantic>=2.7.0
numpy>=1.26.0
